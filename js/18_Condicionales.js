// Condicionales

const disponible = 400
const retirar = 200
const auth = true
if (disponible >= retirar && auth){
    // Se cumple la condicion
    console.log("Si puedes retirar")
}else{
    // no se cumpo la condicion
    console.log("Saldo insuficiente")
}

/**
 *  > - mayor que
 *  < - menor que
 *  >= - mayor o igual que
 *  <= - menor o igual que
 *  == - igual a
 *  === - estrictamente igual a
 *  != - diferente a
 *  !== - estrictamente diferente a
 *  && - operador AND
 *  || - operador OR
 *  ! - operador NOT
 */